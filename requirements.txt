# Supply Chain Monitor Agent — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core LangChain ────────────────────────────────────────────────────────────
langchain==0.3.0
langchain-community==0.3.0
langchain-core==0.3.0

# ── LLM Providers (install all, use only the one you need) ────────────────────
langchain-openai==0.2.0        # For GPT-4, GPT-4o, GPT-4o-mini
langchain-anthropic==0.2.0     # For Claude 3 Haiku, Sonnet, Opus
langchain-google-genai==2.0.0  # For Gemini 1.5 Flash, Pro

# ── Google Search ─────────────────────────────────────────────────────────────
google-api-python-client==2.149.0

# ── Observability ─────────────────────────────────────────────────────────────
langsmith==0.1.130

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1           # Loads .env files
